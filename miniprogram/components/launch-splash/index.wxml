<view
  class="launch-splash {{exiting ? 'launch-splash--exiting' : ''}}"
  aria-role="progressbar"
  aria-label="拼吧正在加载，请稍候"
  catchtouchmove="preventTouchMove"
>
  <image
    class="launch-cover"
    src="../../assets/images/launch-cover.jpg"
    mode="aspectFill"
    data-asset="cover"
    aria-hidden="true"
    binderror="handleAssetError"
  />

  <view class="launch-progress-card" aria-hidden="true">
    <text class="launch-progress-copy">正在拼合活动…</text>
    <view class="launch-track-container">
      <image
        class="launch-groove"
        src="../../assets/images/launch-groove.png"
        mode="scaleToFill"
        data-asset="groove"
        aria-hidden="true"
        binderror="handleAssetError"
      />
      <view
        wx:for="{{blocks}}"
        wx:key="id"
        class="launch-cube-slot"
        style="left: {{item.x}}rpx; top: {{item.y}}rpx; transform: scale({{item.scale}}); z-index: {{item.zIndex}};"
      >
        <image
          class="launch-cube {{item.id <= progress ? 'launch-cube--active' : ''}}"
          src="../../assets/images/launch-cube.png"
          mode="scaleToFill"
          data-asset="cube"
          aria-hidden="true"
          binderror="handleAssetError"
        />
      </view>
    </view>
  </view>
</view>
