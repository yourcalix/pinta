# Plan

1. 扩展社区分页游标，使其携带规范化关键词并拒绝跨关键词复用。
2. 为正式校验器增加最长 30 字的可选帖子搜索词。
3. 在 MemoryStore、CloudStore 与 Mock 中实现仅匹配 ACTIVE 帖子正文的搜索。
4. CloudStore 使用有界多批扫描填页，稀疏命中时继续返回扫描游标，避免漏掉后续结果。
5. 将发现页顶部伪按钮改为真实输入、清空和提交搜索控件；输入态与生效态分离。
6. 搜索结果提示、空态、失败重试、刷新和分页统一绑定 `appliedKeyword`。
7. 更新前后端契约测试与前端规范，运行完整验证。
8. 分别完成 GPT 5.5 后端审查和网页版 Gemini 3.7 Flash 前端审查；若外部模型不可用，如实记录。
