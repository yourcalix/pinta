# 实施计划

1. 将 10 张 RGBA PNG 按业务槽位复制到受控临时目录，以推荐尺寸离线缩放并保留 Alpha，再接入主包资源目录。
2. 首页顶部铃铛/搜索和三张快捷卡改为真实 PNG；Hero 继续保留空插槽。
3. 自定义 TabBar 改为图片图标，维持五等分热区、选中黑色/未选中透明降灰、黄色选中点和中央黑色发布按钮。
4. 为 `activity-card` 新增首页专用 `home-preview` 变体；不修改其他页面使用的 `discover` 和默认变体。
5. 首页活动区改为固定三列 CSS Grid；常规加载显示三卡骨架，追加加载在网格尾部显示一整行三卡骨架，末批不足三张保持左对齐。
6. 保留 limit=3 的 cursor 追加逻辑与并发锁；末页显示统一终点文案，追加失败保留已有内容。
7. 更新专项测试与前端规范，运行定向测试、全量测试、静态检查、包体积检查及微信开发者工具编译。
8. 生成网页版 Gemini 3.7 Flash 前端复审提示词；用户贴回后处理 Critical/Warning，写入 review 并归档提交任务。
