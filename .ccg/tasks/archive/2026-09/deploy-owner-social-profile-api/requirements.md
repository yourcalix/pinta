# 部署要求

- 仅部署 `api` 云函数，不上传小程序版本，不修改数据库或真实业务数据。
- 目标为项目已绑定且历史部署确认过的唯一 CloudBase 环境。
- 使用仓库可重复的平铺 bundle 脚本规避微信 CLI 对嵌套目录的打包缺陷。
- 部署前运行本地验证；部署后回读线上状态，并下载线上包与 staging 包校验一致性。
- 线上包必须包含发起人头像、性别、澳门自然日派生年龄与 MBTI 的公开 DTO 逻辑，同时不得公开完整生日或原始 cloud fileID。
