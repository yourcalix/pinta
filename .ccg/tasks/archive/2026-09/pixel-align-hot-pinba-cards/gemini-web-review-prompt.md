# 网页版 Gemini 3.7 Flash 最终审查提示词

请审查“热门拼吧”发现页活动卡片的最终前端实现。目标是对齐参考图中左侧大幅画面、右侧信息流的比例和字阶，同时不引入参考图里的虚假照片、营销数据或跨日日期。

已实施参数：

- 标准 375px：整卡 `min-height: 390rpx`，左封面/右文案 `46:54`，插画 `230×230rpx` 且 `top: 46rpx`。
- 320px 窄屏：整卡 `min-height: 350rpx`，左封面/右文案 `43:57`，插画 `190×190rpx` 且 `top: 38rpx`。
- 卡片保留自然增高而非固定高度，大字体模式自动转为上图下文并解除文字截断。
- 左侧使用本地白名单 PNG 插画 `aspectFit`；底部遮罩高度 `52%`，渐变加深至 `rgba(6,23,42,0.85)`；底部继续展示真实成员头像和真实容量。
- 右侧日期 `36rpx/900`（窄屏 `32rpx`），标题 `30rpx/700`（窄屏 `28rpx`），地点、发起人与说明均不小于 `24rpx`。
- 说明改为浅灰信息块：`padding: 8rpx 14rpx`、`#f8fafc`、`1rpx #edf1f5`、`10rpx` 圆角。
- 骨架屏同步采用完全相同的 `390rpx + 46:54` / `350rpx + 43:57` 比例，防止加载完成时布局跳变。
- 未修改“我的”页紧凑卡；不显示游玩数、上车人数、虚构照片或跨日时间。
- 自动化与静态检查：`307 pass / 0 fail / 1 historical skip`，项目检查通过。

请按以下格式给出唯一结论，不要提供多套方案：

1. 总体还原度判断；
2. Critical / Warning / Info 分级问题；
3. 每个问题说明触发条件、用户影响和最小 rpx 级修正；
4. 明确检查 375px、320px、大字体模式、长标题/长地点/两行说明、骨架屏到真实卡切换；
5. 区分已能由代码证实的结论与必须真机验证的事项；
6. 最终给出“可以交付 / 修复后交付”。

请特别检查：`46:54` 是否足够接近参考图的画面占比、`390rpx` 是否能承载两行标题与两行说明、左侧插画与底部头像是否会碰撞、辅助文字 `24rpx` 是否仍保持参考图层级。
