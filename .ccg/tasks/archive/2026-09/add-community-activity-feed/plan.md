# Plan

1. Add community-activity types, cursor validation, service actions, and Memory/Cloud store methods.
2. Emit recipient-only reply and post-like activities from the existing atomic write paths.
3. Mirror the contract in the built-in Mock service without seeding fake activity cards.
4. Add the `讨论动态` subpackage page and route the existing Discover bell to it.
5. Add backend and UI regression tests, update CloudBase collection/index documentation, run the full verification suite, and complete CCG review.
