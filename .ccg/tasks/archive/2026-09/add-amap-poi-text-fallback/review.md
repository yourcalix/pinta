# 审查记录

## GPT 5.5 API / 状态机审查

### 初次结论

- Critical：无。
- Warning：原澳门经纬度宽矩形仍覆盖部分横琴/珠海区域；`place/text` 可能返回坐标位于矩形内、但行政区明确属于珠海的同名 POI。
- 其余请求短路、错误脱敏与二级检索触发条件正确。

### 修正

- 对返回 `pname/cityname` 的候选执行澳门行政区校验；显式属于其他地区的候选直接丢弃。
- `place/text` 结果必须带有澳门行政区语义后才允许进入坐标清洗。
- 回归夹具改为坐标仍落在旧矩形内、但 `pname=广东省`、`cityname=珠海市` 的横琴地点，确认不会误收。

## Web Gemini 3.7 Flash

- 分析阶段结论：`APPROVE_PLAN`，同意两层检索状态机、一级失败短路、真实空态与坐标不可用状态区分。
- 最终代码复核：`APPROVE`。
- Critical：无。
- Warning：无。
- 确认请求短路、空态优先级、行政区白名单、矩形围栏、字段兼容与错误脱敏均已闭环。

## 验证

- 专项测试：20/20 通过。
- 全量测试：571 项，570 pass、1 skip、0 fail。
- 工程检查：通过。
