# 需求

- 保持当前拼饭桌页面可见界面、排版、文案和交互入口不变。
- 解决现有 7 项自动化测试冲突，不通过隐藏节点或放宽真实业务断言作弊。
- 清理真机不稳定的 WebP 资产。
- 让拼饭桌当前可见字段通过 Cloud 与 Mock 同构契约真实保存。
- 修复人数范围与 `maxMembers.trim()` 类型冲突。
- 全量测试、语法与结构检查通过后普通推送到 `origin/main`。

## 已有前端审查输入

沿用用户此前贴回的拼饭桌前端审查：专属 food 分支、装饰图触控退出、键盘与 safe-area 防护、真实 DTO 约束。

## 已有后端分析输入

GPT 5.5 建议采用 A+B+C：纯工程问题零视觉修复；当前已批准 food 原稿登记为视觉例外并更新过期断言；正式扩展 food 数据契约并保持旧活动兼容。
