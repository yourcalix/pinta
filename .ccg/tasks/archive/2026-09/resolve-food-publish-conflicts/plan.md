# 实施计划

1. 删除已有 PNG/JPEG 等价物覆盖的 5 个 WebP，并把最后一个纸纹引用迁移到共享 JPEG。
2. 在不改变渲染结果的前提下补齐全局背景宿主、food 独立分支语义、装饰层触控退出和 food 安全区 class。
3. 将人数范围文本从数值 `maxMembers` 中分离，构建稳定的 food 标题、描述与结构化 `typeData`。
4. 扩展 Cloud 校验与 Mock 创建校验，使 payment、性别偏好、MBTI、地图地点和饮食自定义同构；旧记录缺失字段时兼容。
5. 更新发布页草稿白名单和活动详情可见字段，不改变当前发布表单视觉。
6. 将过期的旧版四段式视觉断言替换为当前批准原稿的结构、安全与真实保存断言。
7. 跑定向测试、全量测试、语法与结构检查；执行 GPT 5.5 后端审查，并生成网页版 Gemini 前端复审 Prompt。
8. 修复 Critical/Warning，归档任务、提交，并普通推送 `origin/main`。
