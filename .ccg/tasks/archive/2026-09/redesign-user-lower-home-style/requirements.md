# 我的下方面板视觉更新

用户要求：除上方背景外，下方采用首页和已完成消息页的相同风格。

已读取 pages/user/index.wxml、完整 WXSS、JSON和既有视觉测试。沿用本会话已读取的CCG与小程序设计协作技能，纯前端M级先Gemini分析再实施。

## 明确边界

保留 profile-stage 整体：背景、背景预览、头像、昵称、资料编辑、兴趣、统计和位于背景区的待办横条。保留上方动态安全区、白色状态栏字、图片与JS。只更新 profile-content-sheet 下方分类和活动时间线、空状态，以及原生 bottom 托底色。加载/错误位于上部，若需共享按钮样式应使用下方面板作用域避免污染。

## 方案

- 下方面板 #F9F7F2，保留 -40rpx / 窄屏 -32rpx 重叠和顶部圆角，底部200rpx+SafeArea。
- 四个真实分类保留绑定和数量，深色选中标题，琥珀色标记和浅橙数量底；主文字28rpx、辅助24rpx，数量允许合理换行，控件至少44px热区。
- 时间线保留日期、成员头像、真实人数、状态、封面和导航；暖灰时间线与琥珀节点。
- 活动卡白底、暖灰细边、24rpx圆角、柔和阴影；现有PNG封面与类型语义色保持。
- 标题>=28rpx、元信息>=24rpx，日期/人数/状态容器可换行，时间地点避免整行硬裁切；卡片min-height自适应。窄屏标签换行，必要时缩小封面槽而不裁切原画。
- 空态为同源白卡，黑底按钮保留去发现页原行为。既有加载/错误路径不改。
- 不新增功能、接口、统计、图片，不修改首页、消息、共享TabBar或高德配置。

## 验证与交付

预计改动用户页WXSS、可能需要的局部WXML、JSON bottom托底、对应既有测试和前端规范。无需改JS。复核上方区域diff零改动、320/375px与大字内容、安全区和包预算。运行现有测试与结构检查；实际实现再走Gemini复审，收到反馈后归档交付。
