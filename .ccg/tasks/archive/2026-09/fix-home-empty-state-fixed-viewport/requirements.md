# 需求

- 首页保持 `disableScroll: true` 与单视口固定布局，不恢复上下滚动。
- 首页活动为空或加载失败时，空状态必须完整落在“正在组队”标题与悬浮 TabBar 之间，不被 TabBar 遮挡，也不得从视口底部溢出。
- 空态继续使用既有猫狗 IP、标题、说明和真实操作按钮；触控热区及无障碍语义保持合规。
- 只为首页增加紧凑空态规格；有活动时的三列卡片、骨架屏、Hero 与快捷卡尺寸和比例全部保持不变。
- 覆盖 320px 窄屏、短屏、安全区和系统大字模式的几何防御。
