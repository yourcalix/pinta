# 需求

修复发布页“拼同行”选择公开会合地点后无法在表单中稳定显示的问题。

## 验收

- 选点确认并返回后，表单立即显示地点名称与地址。
- `meetingPoint` 与拼同行出发地字段保持一致，不被页面切换或迟到输入事件清空。
- 用户手动修改出发地时，旧坐标必须失效，避免文字与坐标不一致。
- 草稿保存、返回、再次进入时保持合法地点结构。
- 拼运动、拼饭桌、拼享惠的共用选点能力不回归。
- 页面卸载或选点页无 EventChannel 时安全降级，不出现假成功。
