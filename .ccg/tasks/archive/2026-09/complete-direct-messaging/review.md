# 私信功能补齐：审查与验证

## 当前状态

实现、本地验证与分域终审已完成，按本地代码交付范围归档。界面样式不改；聊天 WXML 仅绑定滚动事件。真实云部署、双账号及真机键盘联调仍未执行，不属于本地测试通过的推论。

## 已采纳输入

用户本轮贴回的 Gemini 分析来自 `/Users/cx/.codex/attachments/42e1d687-0d3e-4dd1-b018-9adc1389c955/pasted-text.txt`。采纳消息合并、保留历史、串行轮询、阅读历史不抢滚动、失败草稿/ID复用和生命周期清理；不采纳不透明 ID 大小比较与 setInterval 重叠请求。

## GPT-5.5 第一轮真实复审

SESSION_ID: `01a06a0d-bb03-79d3-821e-f98600f7f6bd`。

- Critical：无确认问题。
- Warning：已入库消息在 Store 判重前再次审核/限流；修复为鉴权后按确定性 ID 预读并核验 payloadHash，再重放旧结果。新消息仍审核、限流与事务重验。
- Warning：旧已读请求在途时新候选未及时补确认；修复为候选 ID 改变才补请求，防同 ID 网络失败无限循环。
- Info：真实云索引/事务只能通过真实环境验证；文档明确保留待验，不冒充本地契约为云证据。
- Info：徽标已有失败保留测试，但未单独构建完整聊天页与徽标的端到端集成；真实跨页表现仍在真机清单。

## GPT-5.5 第二轮真实复审

SESSION_ID: `01a06a1e-03a8-7742-8935-ba717d614ff4`。

结论：本地代码审查通过；无确认 Critical / Warning；上轮两项问题已关闭。额外确认权限失效递增 loadSeq、清空 fetch/read token 后，晚到列表不会恢复已清空私信。

## 主会话补充边界

- 权限失效与发送/读取并发：加入失败测试后统一作废在途列表。
- 同时间戳下提交最新 ID 未必为排序首项：条件已读采用会话 latest ID，且必须实际取得该正文。保留排序首项作为分页追赶边界；新测试通过。该最后前端修正包含在 Gemini 终审包内。
- README 新增私信集合、真实字段 participantAId/participantBId 的复合索引、云模式不回退 Mock 与双账号联调清单。

## 本地验证

- `npm run verify`：179 项，177 通过、2 既有跳过、0 失败；静态检查 JSON/JS/WXML。
- 覆盖：Memory 权限与幂等；Mock 既有流程；Cloud 模拟查询/事务、305 会话未读扫描；串行轮询、跨页追赶、历史保留、发送重试、编辑保护、隐藏/恢复、已读并发与同时间戳、权限失效晚到响应；徽标乱序/失败/身份隔离；Cloud 接口缺失不回退 Mock、私信正文不进入幂等缓存。
- Cloud harness 将事务序列化模拟，不能证明实际冲突重试；未部署云函数，未改变本地运行模式，未做双真实账号/真机验证。
- npm 提示其版本与本机 Node 18 的支持范围不匹配，命令仍执行成功；本任务未调整全局工具版本。

## 网页版 Gemini 用户贴回的终审

用户已针对 `gemini-web-review.md` 贴回真实反馈，结论为“本地交互代码可交付，真实云与真机待验”。未提出确认的 Critical。

- W-1：部分 Android 首帧可能测得零高度，建议测量前 nextTick。这是待实机复现的布局时序风险，当前已有布局更新后重新测量与 deltaY 降级；本轮不增加未经验证的时序改动，保留在联调清单。
- W-2：第三方键盘二次改变高度时，50ms 聚焦锚定可能提前。保留真机核验，不以任意增加到80–100ms代替真实键盘验证。
- 已确认前端代码审查覆盖：fetch/read/send交错、快速离屏恢复、已读候选变化、阅读历史不跳底、草稿修订保护、失效只读与未读快照。
- 主会话校正反馈中“onHide置空所有Token”的概括：实际只作废fetch/read Token；send Token保留到请求完成以防重发并发，其响应由可见性与loadSeq丢弃。此保留是有意设计，不能为了符合审查措辞而删除。
- 不将审查中对帧率、无泄漏或视觉平滑的概括作为真机证据。

终审后未修改运行时代码；复跑 `npm run verify` 仍为179项、177通过、2既有跳过、0失败，静态检查通过。代码和审查记录本地提交归档，本轮不推送或部署云端。
