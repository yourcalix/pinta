# 修复与验证

- 保留双列 `2 + 1` 自动换行结构。
- 卡片恢复原画廊的 `304:430` 纵横比，标准最小高度恢复为 430rpx。
- 插画恢复 `width: 100%; height: 100%` 的完整画布展示；饭桌素材恢复原有 86% 内缩定位。
- 卡片内边距、标题、说明和“发起＋”恢复原版尺寸，三类说明文案恢复原文。
- 发布专项测试 13/13 通过；全量 144 项中 142 通过、2 项旧链路跳过、0 失败。
- `npm run check` 与 `git diff --check` 通过。
