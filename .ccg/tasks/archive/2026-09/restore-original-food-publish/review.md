# 恢复校验

- `index.js`、`index.json`、`index.wxml`、`index.wxss` 均直接复制自用户 2026-09 微信消息文件区的原始附件。
- 四个目标文件与源文件 SHA-256 完全一致，未进行格式化、字段修复、样式调整或资源压缩。
- `publish.rar` 原样解包到发布素材目录；压缩包内 27 个素材逐文件 `cmp` 一致。
- JavaScript 语法检查、JSON 解析与 `git diff --check` 通过。
- 全量测试结果：326 项，318 pass，7 fail，1 historical skip。7 项失败均来自原始附件与当前工程规范的已知冲突（WebP、旧纸纹/导航结构及此前安全适配版专项断言）。遵照用户“原封不动、不要修改”的明确要求，本任务未修正原件。
