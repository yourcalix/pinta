# 实施计划

1. 收敛首页活动流：保留最多三条首屏请求，移除原地追加和分页状态，标题行新增始终可见的“发现更多 ›”导航入口。
2. 新增 `subpackages/activity/list/index`：使用原生导航栏、暖米白背景、关键词搜索、真实类型筛选、纵向活动卡和游标触底分页。
3. 在 `app.json` 注册活动列表分包页，复用既有 `activity-card` 与 `empty-state` 组件。
4. 增补首页入口、路由防重、全部活动页加载/续页/筛选/错误状态测试。
5. 更新前端规范，运行定向测试、全量校验和微信开发者工具视觉核验。
6. 写入审查结论、归档 CCG 任务并提交。
