# GPT-5.5 后端与隐私分析

- 禁止复用 `selfUser.profile` 到公开活动 DTO；生日、MBTI、兴趣、性别原值继续保持私有。
- 禁止伪造参考图中的评价、评分、职业、接待经验、宠物和自我介绍。
- 推荐新增白名单化的 `ownerProfile` 公共 DTO，至少包含昵称和独立的公开头像槽；保留原 `owner.nickname` 兼容旧端。
- 发起人头像不得由前端依赖 `avatarSlots[0]` 隐式推断，应由后端基于 OWNER 的 ACTIVE member/profile 显式返回。
- 复用现有批量头像水合通道，避免列表 N+1 查询；Cloud、Memory、Mock 必须同构。
- 详情卡的其他丰富信息使用活动本身已经公开的事实：活动类型、状态、当前人数、名额和发布时间。
- 若未来公开 MBTI 或兴趣，必须新增默认关闭、可撤回的显式公开选择，本次不扩大隐私范围。

