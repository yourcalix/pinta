# 实施计划

1. 扩展 Cloud/Memory 现有批量头像水合结果，按活动显式返回 OWNER 的公开头像输入，避免前端依赖成员槽位顺序。
2. 在正式 `publicActivity` 中新增白名单 `ownerProfile`，仅包含昵称和公开头像槽；活动上下文继续复用根 DTO 已公开字段。
3. Mock 同步生成同构 `ownerProfile`，并保持历史数据安全降级。
4. 前端展示装饰层解析发起人头像、活动类型、成团规模、发布时间和终态职责文案。
5. 重构详情页 WXML/WXSS 为“认识发起人”资料卡，加入单次头像破图回退和 320px 适配。
6. 新增正式/Cloud/Memory/Mock 契约、隐私和页面布局测试，运行全量验证。
7. 按 CCG 路由执行 GPT-5.5 后端复审与 Web Gemini 前端复审，处理问题后归档提交。

