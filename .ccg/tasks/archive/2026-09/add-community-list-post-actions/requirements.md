# 需求

- 发现页每张帖子右上角的“三个点”改为真实独立操作入口。
- 当前账号为发帖者时，操作菜单显示“删除内容”；确认后调用既有删除接口，并从当前列表移除该帖。
- 当前账号不是发帖者时，操作菜单显示举报原因并调用既有举报接口。
- 操作入口必须阻止冒泡，不得触发帖子详情跳转。
- 复用详情页已有删除、举报、登录与资料完整性门禁，不修改后端数据契约或页面视觉体系。
- 删除或举报期间需要目标级防重；失败保留现有列表并给出安全反馈。
