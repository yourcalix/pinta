# 审查记录

- 主会话检查：北京时间以 UTC+8 纯 UTC 算术计算，不依赖设备时区；首页通过 `profile.get` 只读加载已有账号资料，不调用会创建账号的 `auth.login`。
- 容灾检查：资料读取失败保留当前称呼，页面隐藏或卸载后通过请求序列丢弃迟到响应，活动列表状态与请求不受影响。
- 自动验证：定向 25 项通过；`npm run verify` 结果为 375 pass、0 fail、1 skip，项目静态检查通过。
- GPT 5.5 路由：连续 190 秒网络连接失败后终止，本次没有伪造外部模型结论。
- 网页版 Gemini 3.7 Flash：审查通过。Critical 无、Warning 无；确认北京时间边界、游客态只读保护、异步请求序列防护及视觉/ARIA 数据绑定均正确，Verdict 为 APPROVE。
