# Web Gemini 3.7 Flash 前端分析

- MBTI 行放在生日之后、更换背景之前。
- 使用复用现有视觉体系的底部抽屉，标题栏为“取消 / 选择 MBTI / 确定”。
- 16 型采用纯四字母 4×4 Chip 网格，避免中文释义造成窄屏拥挤。
- 选择状态使用浅绿色底、翡翠绿边框与文字；提供“暂不设置 MBTI”清空入口。
- 使用独立 `mbtiDraft`：取消或点击遮罩丢弃，确定只回填 `form.mbti`，不直接调用接口。
- 抽屉阻断底层滚动，层级高于固定保存栏，并保留安全区与 320px 窄屏适配。

