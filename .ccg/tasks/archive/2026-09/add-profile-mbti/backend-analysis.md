# GPT-5.5 后端分析

- 新增本人私有资料字段 `mbti`，值为 16 种标准 MBTI 大写枚举之一或 `null`。
- `undefined` 表示旧客户端未提交该字段，更新时必须保留原值；`null` 或空字符串表示用户主动清空。
- 字符串先去除首尾空格并转为大写，再执行枚举校验；非法类型或枚举返回 `VALIDATION_ERROR`。
- `selfUser` 可返回该字段，但活动、社区、群聊等公开 DTO 不得暴露。
- 正式服务与 Mock 服务须保持相同验证、保留和清空语义。
- 前端选择仅写入本地草稿，最终随“保存资料”统一持久化。

