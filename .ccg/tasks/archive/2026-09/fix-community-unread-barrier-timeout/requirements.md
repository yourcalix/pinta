# 需求

- 用户从“讨论动态”打开对应帖子并完成阅读后，返回动态页时，对应动态红点必须消失。
- “全部 / 回复我的 / 收到的赞”的未读数字必须按服务端已读结果减少。
- 发现页铃铛与消息页讨论动态入口读取同一权威未读汇总。
- 弱网下不得用旧快照覆盖已完成的已读写入；失败或 `stale` 仍保持未读。
- 等待已读写入期间保留现有列表内容，不清空页面或制造白屏。

# 已确认根因假设

当前服务层的 2 秒屏障会在真实云函数写入仍在途时提前放行后续列表和未读读取。旧快照先返回后，没有尾随刷新，因此红点与数字可长期保持旧值。
