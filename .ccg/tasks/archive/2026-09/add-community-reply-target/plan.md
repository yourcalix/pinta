# 实施计划

1. 扩展回复创建参数与存储层事务校验，支持可选 `replyToId` 且禁止跨帖、失效目标。
2. 调整回复动态接收人：普通回复通知帖子作者，定向回复通知目标评论作者，自回复不通知。
3. 扩展公开回复 DTO，批量装配安全的 `replyTo` 展示信息，并为删除目标提供不可用降级。
4. 同步小程序 mock 与 community service，保证本地联调和真实云端契约一致。
5. 在讨论详情页实现点击评论、定向回复提示、取消、动态占位符与发送状态闭环。
6. 补齐后端、mock、前端交互与静态结构测试，执行完整测试并按 CCG 流程审查、归档。
