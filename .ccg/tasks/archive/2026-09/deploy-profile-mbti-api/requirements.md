# 部署要求

- 将提交 `5c03ce7` 中支持可选 MBTI 的 `cloudfunctions/api` 部署到项目已经绑定的 CloudBase 环境。
- 部署前重新确认真实 AppID、唯一环境、函数状态和本地测试结果，不猜测部署目标。
- 使用已验证的平铺 bundle 流程规避微信 CLI 递归目录读取问题。
- 部署后下载线上函数，与本地部署包逐文件校验，并确认 MBTI 枚举、保留/清空语义和本人 DTO 已进入线上代码。
- 不读取、修改或清空真实业务数据。
