# 审查记录

## GPT-5.5 后端/安全审查

- 首次审查因隔离沙箱无法读取工作区而阻断；第二次将实际后端补丁通过标准输入提供后完成有效审查。
- 已修复：生产人工审核不再依赖“必须显式开启”的脆弱配置，默认开启并允许通过 `ENABLE_STUDENT_REVIEW=false` 显式关闭。
- 已修复：生产环境不再回退复用司机认证密钥；仅 development/test 可临时回退，生产缺少独立 `STUDENT_CREDENTIAL_SECRET` 时认证提交 Fail-Closed。
- 已修复：学生校区配置复制为独立策略快照，避免与拼车校区常量形成同一可变业务边界。
- 已补测试：生产误开自动通过仍保持 `SUBMITTED`；跨用户上传确认被拒绝；公开 DTO 不含学号原文和私有文件路径；客户端不持久化认证表单。
- 已核实无须修改：`requireVerifiedStudent` 明确要求 `status === 'APPROVED'`；人工审核同时要求开关与 `admin` 角色；公开 DTO 仅返回脱敏摘要。
- 已核实 Cloud 文件确认：采用随机封存路径，事务竞争失败或重复确认产生的冗余封存文件在 `finally` 中删除，原始上传文件同样清理；该实现与既有司机认证封存链路一致，不存在审查摘要所推断的未清理孤儿文件。

## 验证

- `npm run verify`：235/235 通过。
- 静态检查：JSON 107、JS 101、WXML 19，状态 `ok`。
- 运行环境提示：npm 11 对当前 Node 18 给出版本警告，但测试与静态检查均成功退出。

## 待完成

## 网页版 Gemini 3.7 Flash 前端审查

- 结论：无 Critical，整体【可交付】。
- W-01 已修复：`APPROVED` 状态认证卡收敛为最小 88rpx 的紧凑 Banner，隐藏重复说明，保留完整触控热区与认证状态语义。
- W-02 经源码核实已实现：锁定卡说明通过 `dashboardMode === 'driver'` 在乘客行程与司机资格/车辆/承接任务之间同步切换，无须重复修改。
- I-01 已优化：上传成功按钮明确显示“已上传，点击可重新选择”，原点击上传链路可直接覆盖误选照片。
- Gemini 对焦点顺序、五状态矩阵、隐私说明、320px、大字号、软键盘与 Safe Area 均未发现阻断问题。

## 最终验证

- 应用前端审查修正后再次执行 `npm run verify`：235/235 通过。
- 静态检查：JSON 107、JS 101、WXML 19，状态 `ok`。
- 真机待验：iOS/Android 相册/相机调起与上传；iPhone SE/Android 异形屏键盘推升；VoiceOver/TalkBack 状态卡与表单焦点顺序。
