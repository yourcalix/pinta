# CloudBase 开发环境切换复核

## 已完成

- 私有运行配置已切换到用户指定的免费开发环境，默认仓库配置仍保持 Mock。
- 私有 `miniprogram/config/local.js` 已由 `.gitignore` 隔离。
- 按用户明确授权清空旧配送业务集合，并创建拼吧所需的 15 个集合。
- 正式 `api` 云函数部署成功。
- 真实 `activity.list` 调用返回 `ok: true`、空列表和空游标。
- 发现页真实请求从加载失败恢复为“暂时没有拼车行程”。
- 一次性初始化函数已重新部署为固定 `DISABLED` 响应，本地初始化源码已移除。
- 云端 `api` 超时已从 3 秒调整为 15 秒，并通过 CLI 回读确认。
- 已在云函数环境变量中设置开发专用 `DRIVER_CREDENTIAL_SECRET`；随机密钥未输出、未落盘、未进入 Git。
- 已设置 `PINBA_ENV=development`、`ENABLE_RIDE_DRIVER_ACCEPTANCE=true`、`ENABLE_DEV_DRIVER_REVIEW=true`。
- 配置更新后重新加载发现页，真实 CloudBase 请求仍正常返回空行程状态。
- `npm run verify` 通过：168/168 测试及项目结构检查成功。

## 已观察问题

### Warning

- 当前云端 `api` 仍为 Node.js 16.13；本次功能正常，但上线前应评估升级到受支持的较新运行时。
- 微信开发者工具 CLI 对带 `lib/` 子目录的函数打包时报 `EISDIR`；本次使用内容等价的临时平铺副本完成部署，后续应建立可重复的正式部署脚本。

### Info

- 云端仍保留已禁用的 `pinbaInitDev` 函数；其调用固定返回 `DISABLED`，不再具备清库能力，可在控制台维护窗口中删除。
- npm 报告 npm 11.7.0 与本机 Node 18.20.8 的版本警告，但本次验证成功且不影响产物。

## CCG 外部复审

- GPT-5.5 reviewer 首次调用超过两分钟无任何输出；授权项完成后又以最小审查输入重试 60 秒，仍无输出。两次均已终止并如实记录，未伪造外部模型结论。

## 当前结论

- 指定免费 CloudBase 开发环境切换、数据结构初始化、API 部署和司机开发开关配置均已完成。
- 当前环境可继续进行澳门校园拼车的司机注册、开发审核与承接流程联调。
