# 司机认证首次提交故障复核

## 根因

- 首次提交时 `driverApplications/{userId}` 尚不存在。
- `CloudStore.submitDriverApplication` 在 CloudBase 事务内直接调用 `doc(userId).get()`；真实环境对不存在文档抛出 `errCode = -502005`。
- 该 SDK 异常未被归一化，最终通过安全错误边界返回 `INTERNAL`，页面显示“服务暂时不可用”。
- 目标开发环境此前已配置独立 `DRIVER_CREDENTIAL_SECRET`，因此本次现象与缺失加密密钥的稳定业务错误不一致。

## 修复

- 首次读取改用项目已有的 `getTransactionDocument`。
- 仅将明确的 `-502005` / 文档不存在语义转换为 `null`；权限、网络、事务和其他 SDK 异常继续向外抛出。
- 申请、敏感记录、上传绑定和审计仍在同一 CloudBase 事务中完成，没有放宽证件加密、上传检查或权限规则。

## 测试

- 首次申请不存在时可原子写入申请、加密敏感记录、审计并绑定已检查上传。
- 非 `-502005` 事务错误继续原样抛出。
- 后段审计写入失败时不提交任何申请、敏感记录或上传绑定。
- 司机认证专项：14/14 通过。
- 全量自动化：186/186 通过。
- `node --check cloudfunctions/api/lib/cloud-store.js` 通过。
- `git diff --check` 通过。

## GPT-5.5 复核

- Critical：无。
- Warning：要求确认只吞掉明确不存在错误，并补充非缺失异常与事务失败原子性测试；两项已补齐并通过。
- Info：凭据密钥应继续只存在云函数环境变量，不进入数据库、日志、错误响应或仓库；当前保持该约束。

## 云端部署

- 环境：`cloud1-d0giupmx3ce04ddd0`
- 云函数：`api`
- 部署结果：成功，15 个文件，37.5 KB。
- 数据库：未删除、覆盖、迁移或手工读取用户认证资料。

## 结论

【可交付】。需由原提交用户在真机上直接再次点击“提交申请认证”，客户端会复用此前保留的幂等键安全重试。
