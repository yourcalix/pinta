# 审查结果：活动分享卡片与冷启动直达

## 结论

【代码阶段可交付】

- Critical：无。
- Warning：无。
- Info：`onShow` 返回的 Promise 会被微信页面生命周期忽略；当前写法不影响运行，并便于 Node 测试等待公开详情加载完成，无需修改。

## CCG 审查证据

- 主会话完成路由参数、公开身份边界、分享 payload、错误分支、请求序号和弹性布局审计。
- GPT 5.5 architect 已按配置调用，但外部通道在限定等待 90 秒内没有返回正文；已终止进程，不把无返回结果记为分析通过。
- 用户两次从网页版 Gemini 3.7 Flash 贴回真实结果：分析阶段结论“调整后可执行”；实现后复审 Critical 无、Warning 无，最终结论“可交付”。

## 关键判断

1. 当前 `App.onLaunch` 没有异步身份恢复，公开 `activity.detail` 不依赖登录，因此不引入全局 readiness Promise。
2. 缺失或畸形 ID 会安全进入 `NOT_FOUND` 并结束骨架屏；`NOT_FOUND` 与 `TAKEDOWN` 均只允许返回发现页。
3. 成功分享只返回公开标题和编码后的当前活动 ID；任何 loading/error/无活动状态均降级分享发现页。
4. 页面内 `open-type="share"` 按钮与右上角原生分享菜单并存；按钮使用 88rpx 最小热区、读屏标签和可换行标题布局。
5. `_loadSeq` 继续截断卸载后响应与旧请求覆盖，不改变申请、管理或举报权限边界。

## 自动化结果

- 分享冷启动专项：5/5 通过。
- 详情错误相关组合：9/9 通过。
- `npm run verify`：67/67 通过，项目结构检查通过。
- `git diff --check` 与 JavaScript 语法检查：通过。
- `config.useMock`：保持 `true`。
- npm 仍提示 npm 11.7 与 Node 18.20.8 版本声明不匹配，但所有验证命令退出码为 0。

## 外部验收保留项

- iOS：杀死微信进程后从聊天卡片冷启动、单页栈返回发现、右上角分享卡片标题与封面、VoiceOver。
- Android：冷启动卡片、全面屏手势、大字号下标题与分享按钮换行、TalkBack。
- 上述真实 AppID/真机项尚未执行，不伪记为通过；不影响 Mock 下代码阶段交付。
