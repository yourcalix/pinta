# 审查结论

## 根因

- `decorateActivity` 对所有拼车状态无条件按剩余名额改写为“招募中 / 已满员”，覆盖了服务端返回的 `CANCELLED` 等生命周期状态。
- “我的”页面缺少最新请求保护，慢速旧请求存在覆盖新状态快照的竞态风险。

## CCG 交叉审查

- GPT 5.5 后端 Debugger：确认取消接口与 `activity.mine` 以服务端状态为事实源，建议限制展示层人数映射范围，并增加 Dashboard 请求序号。
- Gemini 3.7 Flash 前端审查（由用户粘贴真实结果）：确认已取消帖子应保留在“我的发布”作为历史记录并显示“已取消”，同时认可最新请求获胜策略。
- GPT 5.6 Sol 综合：按最小范围同时修复状态优先级和异步竞态，不隐藏历史帖子，不改取消接口契约。

## 验证

- 定向测试：9/9 通过。
- 全量测试：194/194 通过。
- `node --check`：通过。
- `git diff --check`：通过。
- Critical：无。
- Warning：无。
