# iPhone 启动页资源兼容修复复审

## CCG 复审来源

- 前端分析与实现复审：网页版 Gemini 3.7 Flash，用户于 2026-08-26 两次贴回真实结果。
- 主会话综合、实现和验证：GPT 5.6 Sol。
- 本任务不修改后端、数据库、鉴权或 API 契约，无需 GPT 5.5 后端复审。

## 结论

【可交付】

- Critical：无。
- Warning：无。
- Info：透明调色板 PNG 的边缘需以 Retina 真机结果为准；本次 iPhone 实测封面、凹槽与方块均正常出现。

## 已验证

- 组件图片全部使用 `../../assets/images/` 静态相对路径。
- 封面替换为 baseline JPEG，透明凹槽和方块替换为标准 PNG，总计 183343 bytes。
- 三张图片均绑定 `binderror`；组件对并发资源错误只上报一次。
- 父页面收到错误后立即清理计时器、关闭启动层并恢复 TabBar。
- 自动化 127/127；项目结构检查和 `git diff --check` 通过。
- iPhone 真机预览确认封面、黑色凹槽和绿色落块正常出现。

## 后续观察

- Android 弱网/断网下确认本地主包资源仍即时显示。
- Retina 屏观察透明 PNG 阴影边缘是否存在可见硬边；无明显问题则不切换至体积更大的 RGBA PNG。
