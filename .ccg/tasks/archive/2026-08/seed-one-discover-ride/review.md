# 交付复核

## 结果

- 已在 `cloud1-d0giupmx3ce04ddd0` 创建一条澳门校园拼车测试行程。
- 发现页真实 CloudBase 列表显示 `1 个行程`，路线为“青茂口岸 → 凼仔校区”，状态为“招募中”。
- 验证截图：`discover-one-ride.png`。

## CloudBase 兼容性修复

1. 用户历史文档的 `profile` 为 `null` 时，嵌套字段更新会失败。现改为事务读取完整用户文档后整字段替换 `profile`，并保留账号状态、角色、引导状态和创建时间等其他字段。
2. CloudBase 事务中的 `doc(id).get()` 在目标活动不存在时会抛出 `-502005`，而不是返回空数据。现只在活动幂等槽位读取边界将明确的“文档不存在”错误归一化为 `null`，其他异常继续抛出。
3. 临时诊断 token、原始错误输出和客户端诊断日志已全部移除，正式云函数已重新部署。

## 验证

- `npm test`：177/177 通过。
- `npm run check`：通过，项目结构状态 `ok`。
- 新增回归测试覆盖：`profile:null` 初始化、事务缺失活动文档、跨集合创建写入。
- 人工实测：资料保存、行程发布、详情加载、发现页公开列表均成功。

## 审查

- 主会话复核：未发现 Critical/Warning；事务读取后整文档写回保持并发隔离，缺失文档错误识别只用于确定性活动 ID 的幂等槽位。
- CCG 指定的 GPT-5.5 后端复审连续两次未能返回结果：第一次 MCP 握手超时且回退模型触发用量限制，第二次长时间无输出后终止。未伪造外部模型结论；该外部服务故障不影响已完成的自动化与真实 CloudBase 验证。
